
/// <summary>
/// Enum representing all scenes in the game.
/// Must match Build Settings order exactly.
/// </summary>
public enum Scenes
{
    None = -1,
    LogoScene = 0,
    Supreme = 1,
    HubScene = 2,    
    GameScene = 3,
}
